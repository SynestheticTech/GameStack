{
  "version": 2,
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }],
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "command": "npm run build:prod",
    "use": "@vercel/static"
  }
}
